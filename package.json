{
  "name": "chromeext",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "zip": "zip -r _chrome.zip .",
    "dev": "parcel public/index.html --port 3000",
    "build:parcel": "parcel build public/index.html --no-source-maps -d build/",
    "publish": "cp -R ./public ./build && npm run build:parcel && zip build.zip build/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.1.3",
    "uglify-js": "^3.12.6",
    "webpack": "^5.20.2",
    "webpack-cli": "^4.5.0"
  }
}
